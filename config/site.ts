export type SiteConfig = typeof siteConfig;

export const siteConfig = {
	name: "Shortar | Keep it Short, Store it Smart and Forever",
	description: "Keep it Short, Store it Smart and Forever.",
	navItems: [
	],
	navMenuItems: [
	],
	links: {
		github: "https://github.com/nextui-org/nextui",
	},
};
